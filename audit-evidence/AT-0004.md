# AT-0004 — Presentation Foundation, Public Trust Pages & Build Repair (evidence summary)

- **Branch:** `presentation/public-trust-pages`; **pre-commit HEAD:** `31dc916` (merge of `origin/main` `404ea77`).
- **Date:** 2026-09-13 (verification/classification + commit session).
- **Authority:** `docs/CURRENT_STATE.md` → AT-0004; `docs/DECISION_LOG.md` → D-0007; `docs/CHANGE_LOG.md` → AT-0004; `docs/BACKLOG.md` → AT-0004.

## Current-state evidence (this directory)

- `working-tree-classification.json` — per-file classification of the working tree vs HEAD (regenerated 2026-09-13, final run).
  Method: `git status --porcelain=v1 -z`; for each modified file compare `normalizeEOL(work)` against `prettier.formatSync(normalized HEAD blob)` using the repo `.prettierrc` (`endOfLine:lf`). Equal ⇒ `FORMATTING_ONLY`; different ⇒ substantive (`REAL_DIFF`/manual-inspect).
  Result: 258 `FORMATTING_ONLY`, 7 `REAL_DIFF` (`package.json`, `src/routes/__root.tsx`, `src/routeTree.gen.ts`, `docs/BACKLOG.md`, `docs/CHANGE_LOG.md`, `docs/CURRENT_STATE.md`, `docs/DECISION_LOG.md`), 2 `UNPARSEABLE` (`.gitignore`, `.prettierignore`), 2 untracked (`pnpm-lock.yaml`, `pnpm-workspace.yaml`). NOTE: the classifier lumps the 5 `docs/archive/*(2).md` files into NEW (git quotes paths with spaces; `git show HEAD:<path>` fails) — `verify-quoted-docs.mjs` proves they are formatting-only, so they are counted in the 263-file formatting commit.
- `classify-worktrees.mjs`, `verify-quoted-docs.mjs` — the classification scripts (re-runnable).

## Verification (current-session runs)

| Check                          | Result                                              |
| ------------------------------ | --------------------------------------------------- |
| Merge/conflicts                | `git ls-files -u` = 0; 3 conflicts resolved         |
| TypeScript (`tsc --noEmit`)    | 0 errors                                            |
| Prettier (`prettier --check .`) | clean (exit 0)                                     |
| Lint (`pnpm lint`)             | 112 problems (74 err / 38 warn) — known baseline    |
| Build (`pnpm build`)           | SUCCESS (exit 0, ~29 s) after `cross-env` correction |

## Dependency configuration status

- `package.json` `pnpm.overrides.entities` = `4.5.0` → **RESOLVED — NOT REQUIRED, dead `pnpm.overrides` field REMOVED from `package.json`**. No documented engineering reason for the pin (gpt-engineer scaffold artifact `b0ac8db`); no `src/`/`test/` code imports `entities` directly; Bun resolves `entities@4.5.0` naturally for `dom-serializer@2.0.0`, `htmlparser2@8.0.2`, `@react-email/render@2.1.0`; `parse5@7.3.0` keeps its own nested `entities@6.0.1`; `vite.config.ts` aliases compensate for subpath differences (lines 92–107). Clean Bun install: `bun.lock` entities unchanged (4.5.0 top, 6.0.1 nested in parse5).
- `pnpm-workspace.yaml` (untracked): `allowBuilds` only; no `overrides`.
- **Package-manager decision (D-0007): STOP — bun is authoritative; pnpm NOT adopted.** Untruncated evidence: `README.md` ("managed with **bun**", `bun.lock`, `bunfig.toml`), `DEPLOYMENT.md` (bun commands, `oven/bun:1` Dockerfile with `bun install --frozen-lockfile`, `bunx wrangler deploy`), canonical tracked `bun.lock` + tracked `bunfig.toml` (`minimumReleaseAge=86400`), `docs/AUDIT_BASELINE_AT-0001.md` ("Package manager: `bun`"), tests via `bun:test`, no `packageManager` field in `package.json`. Only pnpm signal = dead `pnpm` field (ignored by pnpm 11) + untracked `pnpm-lock.yaml`/`pnpm-workspace.yaml`. Contradiction → NOT DECIDED; pnpm lockfiles **excluded** from the AT-0004 commits.

## Commit record

- Commit 1 (substantive, 13 files): `package.json`, `.gitignore`, `.prettierignore`, `src/routes/__root.tsx`, `src/routeTree.gen.ts`, the four governance docs `docs/{BACKLOG,CHANGE_LOG,CURRENT_STATE,DECISION_LOG}.md`, and this directory (`AT-0004.md`, `working-tree-classification.json`, `classify-worktrees.mjs`, `verify-quoted-docs.mjs`).
- Commit 2 (formatting-only, 263 files): all files proven `working tree == prettier.format(HEAD)` — 258 classifier `FORMATTING_ONLY` + 5 `docs/archive/*(2).md`.
- `pnpm-lock.yaml`/`pnpm-workspace.yaml`: remain untracked (excluded, D-0007).

## Working tree (committed)

- Pre-commit: 272 ` M` + 3 `??`. Post-commit: only `pnpm-lock.yaml` + `pnpm-workspace.yaml` untracked.